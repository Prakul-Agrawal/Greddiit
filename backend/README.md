Before running, create a `.env` file. It should have the following:

- `MONGODBURI`, to connect to MongoDB database
- `SECRETKEY`, for hashing the passwords
- `PORT`, to connect to the specific backend port
